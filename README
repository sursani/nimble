Nimble project:
Nice little node app to see tweets from a selected bunch of tweeters. We can create categories i.e. Celebrity tweets, NBA tweets, NFL tweets, etc.

Collaborators:
Allan Pasco
Shane Ursani

You to need to run the following for it to all work:
npm install express
npm install ejs
npm install oauth
npm install mongoose
