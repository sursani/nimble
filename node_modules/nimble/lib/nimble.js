var categories = ['celebrity', 'nba', 'nfl'];

module.exports = {
	
	isValidCategory: function (cat) {
	    return (categories.indexOf(cat) != -1);
	},
	
	getAllCategories: function () {
		return categories;
	}
	
};